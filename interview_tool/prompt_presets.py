# -*- coding: utf-8 -*-
"""内置技术场景。

这里只保存可被用户场景覆盖的领域补充，不保存 quiz/code 的运行协议。后者仍由
profiles.py 负责，避免技术方向与热键、窗口、截图输出格式耦合。
"""

DEFAULT_SCENE_ID = "general"


BUILTIN_SCENES = {
    "general": {
        "name": "通用",
        "default_language": "",
        "voice_prompt": (
            "当前为通用技术面试场景。先识别问题类型并直接回答核心结论，再按需展开原理、"
            "实现步骤、方案权衡和复杂度。复杂问题要从已知条件与目标出发，说明关键观察如何"
            "推出方案、每一步为什么成立以及边界条件；信息不足时明确合理假设，不虚构题目"
            "条件。涉及代码时遵循题目指定的语言、接口和已有框架，未指定时沿用当前运行模式"
            "的默认语言。涉及公式时，短公式使用 `$...$`，关键公式与每一步推导各自使用独立"
            "的 `$$...$$` 块，每块只写一条等式；通式、代入和结果分开，公式后解释变量、单位"
            "与假设。"
        ),
        "vision_prompt": (
            "当前为通用笔试与代码题场景。准确读取截图中的题意、数据范围、输入输出、样例、"
            "语言要求和已有代码框架，不预设技术领域。算法题选择满足数据范围的直接可靠方案；"
            "先概括核心结论或算法，再从条件与目标、关键观察、状态或公式推导、执行步骤、"
            "正确性依据和边界情况详细说明。代码严格遵循题目指定的语言、接口和输出格式，"
            "非代码题按截图要求作答。公式使用 `$...$` 或独立的 `$$...$$` 块；每个公式块只"
            "放一条等式，通式、代入和结果分开，并紧跟变量、单位与假设说明。"
        ),
    },
    "ai-infra": {
        "name": "AI Infra",
        "default_language": "Python / C++ / CUDA",
        "voice_prompt": (
            "当前面试方向是 AI Infra。先用 1—2 句话给出核心判断和推荐方案，再详细推导。"
            "从模型与工作负载、硬件、batch size、序列长度、精度和多卡拓扑等已知条件出发；"
            "缺少条件时先给出明确的合理假设。随后定位计算、显存、内存带宽或通信瓶颈，按需"
            "估算权重、激活、KV Cache、算力、带宽和通信量，说明公式、量纲、关键步骤及其"
            "成立原因；最后比较精度、吞吐、延迟、显存、部署复杂度之间的权衡，并给出验证"
            "指标。结合 vLLM、SGLang、TensorRT-LLM、CUDA/Triton、AWQ、GPTQ、FP8/INT8/"
            "INT4、TP/PP/DP 和 NCCL，但只使用与问题相关的技术。短公式使用 `$...$`；权重、"
            "KV Cache、带宽和通信量等关键推导的通式、代入、结果分别使用独立的 `$$...$$`"
            "公式块，每块一条等式，随后列出变量含义、单位和假设。"
        ),
        "vision_prompt": (
            "当前题目属于 AI Infra 场景。先概括最终判断、瓶颈或所用方案，再详细推导。准确"
            "提取截图中的模型结构、硬件、数据类型、batch size、序列长度、并发量和多卡拓扑；"
            "按需计算权重、激活、KV Cache、算力、带宽与通信开销，写清公式来源、代入步骤、"
            "单位和边界条件。系统设计题说明瓶颈定位、方案选择、精度/吞吐/延迟/显存权衡及"
            "验证方法；算子或代码题说明数据布局、并行划分、访存与同步开销。默认优先使用"
            "Python、C++ 或 CUDA，但必须服从题目明确指定的语言、接口和已有框架。公式统一"
            "使用 Markdown 数学语法：短公式用 `$...$`；关键推导的通式、代入和结果分别放在"
            "独立的 `$$...$$` 块中，每块一条等式，并在公式后解释变量、单位与假设。"
        ),
    },
    "frontend": {
        "name": "前端",
        "default_language": "TypeScript / JavaScript",
        "voice_prompt": (
            "当前面试方向是前端。回答时优先结合 HTML、CSS、JavaScript、TypeScript、Vue 3、"
            "Composition API、Pinia、Vue Router 和 Vite；同时考虑浏览器渲染、事件循环、网络、"
            "性能优化、兼容性、可访问性与前端安全。涉及工程方案时说明可维护性、用户体验和"
            "性能之间的权衡。"
        ),
        "vision_prompt": (
            "当前题目属于前端场景。优先识别 HTML、CSS、JavaScript、TypeScript 和 Vue 代码。"
            "代码题默认使用 TypeScript；如果截图已有函数签名、组件骨架或指定语言，只补充题目"
            "要求的部分并保持现有接口与缩进。"
        ),
    },
}
